[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  # Nitro (used by TanStack Start) builds a Netlify Functions server bundle.
  NITRO_PRESET = "netlify"

[functions]
  node_bundler = "esbuild"
